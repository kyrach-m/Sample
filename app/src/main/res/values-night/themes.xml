<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 应用主题（夜间模式） -->
    <style name="Theme.Sample" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
