<?xml version="1.0" encoding="utf-8"?>
<!--
    core:ui 统一主题样式（Design System）
    业务模块的 Activity/Application 可继承此主题。
-->
<resources>

    <!-- ==================== 应用主题 ==================== -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorOnBackground">@color/on_secondary</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorOnError">@color/on_error</item>
    </style>

    <!-- ==================== 对话框样式 ==================== -->
    <style name="CoreDialogStyle" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="shapeAppearanceOverlay">@style/CoreDialogShape</item>
    </style>

    <style name="CoreDialogShape">
        <item name="cornerSize">@dimen/dialog_corner_radius</item>
    </style>

    <!-- 底部弹出对话框样式 -->
    <style name="CoreBottomSheetStyle" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CoreBottomSheet</item>
    </style>

    <style name="CoreBottomSheet" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="shapeAppearanceOverlay">@style/CoreBottomSheetShape</item>
    </style>

    <style name="CoreBottomSheetShape">
        <item name="cornerSizeTopLeft">@dimen/radius_l</item>
        <item name="cornerSizeTopRight">@dimen/radius_l</item>
    </style>

</resources>
